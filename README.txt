To allow the to the database; configure the .env variables, ensuring the username and password are correct.

To build, simply run the makefile with "make build"

To run, use the makefile with "make run"

to view git commits, enter "git log" in terminal;

Github:
https://github.com/u24916031-Jamie/COS221-PA4

user xd-King is me, u24916031