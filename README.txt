To allow the program to the database; configure the .env variables, ensuring the username and password are correct.

To build, simply run the makefile with "make build"

To run, use the makefile with "make run"

